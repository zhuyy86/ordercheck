# 用自己的话讲清楚 OrderCheck

你不需要逐行背代码。先能在电脑上操作，再用下面的解释理解数据为何得到这样的结果。本文是学习材料，不能代替你实际演练，也不代表你已经掌握全部技术。

## 1. 项目到底解决什么问题

不同门店的订单表可能把同一列叫“金额”或“Total”，日期格式也不同。直接拼在一起会出现错列、重复和错误数字。
我们的程序先把字段对齐，再判断每条订单是否合格，把有问题的订单单独保留，并让你找到原文件中的位置。

客户能使用的是一个反复运行的程序：下个月换一批表格，继续按同一套规则处理。

## 2. 怎么用与怎么运行

本机在 repo 文件夹双击 start-ordercheck.cmd，再打开 http://127.0.0.1:8501 。程序窗口要保持打开。
先使用“合成示例”，点击校验。应该看到 12 条输入、5 条通过、7 条待复核/重复、金额 140.30。
依次看“通过记录”“待复核记录”“修改记录”“运行依据”，最后下载 Excel。

后台程序仍在你的电脑上运行。浏览器只是操作面板，数据没有发送给 AI 服务。
命令行入口完成同一件事，适合未来批量自动执行；它与页面共用同一套规则，避免两套结果不一致。

## 3. 程序的五步流程

文件→列名映射→标准化与校验→通过/异常分流→报告。

读入时先检查文件结构，映射把不同名称对应到四个标准字段。标准化只做明确转换，例如 2026/09/08→2026-09-08、30→30.00。校验判断必填、日期和金额，不会把“待确认”猜成 0。
同门店和订单号重复时，若业务内容相同，保留第一条，其余进入重复清单；如果内容不同，则整组待复核，不能随便选一个。

每次变更都会留下字段、变更前后和来源。每行 raw_json 存着原始字段文本，方便追查，原文件本身仍需保存。

## 4. 文件夹干什么

- src：真正执行任务的程序。
- config：可调整的业务规则，类似机器的“办事标准”。
- examples：专门用于演示的合成表格。
- tests：给程序准备的检查题，改代码后重复验证。
- docs：实际运行截图。
- outputs：你下载或命令行生成的报告，默认不上传 GitHub。
- .venv：运行程序用的本机环境，不是作品源代码，不上传。

## 5. 最重要的 10 个文件

| 文件 | 一句话作用 |
|---|---|
| src/ordercheck_app.py | 你在浏览器看到的按钮、映射选择和结果表 |
| src/ordercheck_cli.py | 不用点按钮也能执行批次的入口 |
| src/order_batch.py | 文件读取、字段对齐、来源记录、报告导出 |
| src/order_rules.py | 判断订单合格、重复还是冲突的规则 |
| src/data_cleaning.py | 上游的清洗类；新增订单入口，管理原数据、结果和变更历史 |
| config/orders.json | 默认列名别名、金额范围和重复键 |
| src/order_demo.py | 把合成表格组成演示，第三份转为 Excel |
| tests/test_order_batch.py | 检查混合文件、映射和报告内容 |
| tests/test_app_cli.py | 检查界面流程和命令行行为 |
| UPSTREAM.md | 说明代码来源、原作者和我们的修改 |

## 6. 上游是什么，我们新增了什么

原项目是一个通用单表清洗工作台，已有缺失值处理、异常值分析、可视化和导出。
我们保留它的 DataCleaner、工具、旧界面及许可证声明，并记录固定上游提交；在原清洗类增加订单校验入口。新文件实现多表读入、规则配置、字段映射、跨文件重复冲突检查、审计报告、中文工作台和 CLI。

所以不能说“所有代码都是我从零写的”。准确说法是：“我选择了一个 MIT 开源清洗工具，在 Codex 辅助下完成了面向多门店订单的扩展，并用自动测试和实际演示验证。”
如果被问到具体编码分工，应如实说明 Codex 承担了主要代码编写；你提出了任务目标和要求，目前需要通过演练确认自己的理解。

## 7. 几个技术名词

- pandas：把表格作为可编程的数据来处理。
- openpyxl：读取 Excel 的工具。
- Streamlit：用 Python 做浏览器操作面板。
- Decimal：按十进制处理金额，避免把浮点误差当业务数字。
- JSON：规则和记录的一种文本存储格式。
- SHA-256：文件内容的指纹，用来核对是不是同一份输入，不是加密文件。
- pytest：自动运行检查题。测试通过说明已覆盖的情况符合预期，不保证世界上没有任何 Bug。
- Git / GitHub：前者记录代码修改，后者用于公开展示。本项目已直接发布到 `https://github.com/zhuyy86/ordercheck`，不是 Fork。

## 8. 面试最可能问什么

| 问题 | 你需要理解的答案 |
|---|---|
| 为什么不直接用 Excel 合并？ | 单次手工操作可以做，但重复数据和业务错误需要一致规则与可追踪记录；本程序把流程保存成可重复运行的代码 |
| 与原项目有什么不同？ | 从通用单表清洗扩展为多文件订单验收；不是换名字或翻译界面 |
| 为什么不自动修正所有错误？ | 订单金额和日期关系到业务事实，没有依据不能猜测，所以未知值进入待复核 |
| 怎样处理重复订单？ | 默认用门店+订单号匹配；完全一致才去重，内容冲突整组隔离；不同门店同号不是重复 |
| 如何保留 001？ | 读入与处理把文本订单号当字符串；如果原 Excel 已存成数字 1，则无法恢复已丢失的零 |
| 金额怎么准确？ | 用 Decimal 校验和求和，明确最多两位小数；不同币种不能直接合计 |
| 文件坏了怎么办？ | 文件结构问题阻止整个批次并报出来源；行级业务值问题进入异常表；不默默跳过文件 |
| 怎么证明结果正确？ | 合成输入有已知答案，自动断言输出、异常数、合计、来源；再用真实浏览器上传和下载核对 |
| 规则改了以后呢？ | 旧结果隐藏，要重新执行；报告包含当次规则、映射和输入指纹 |
| 如何回溯某条错误？ | 看 rejected 的 reason，再按 source_file/source_sheet/source_row 回原表；raw_json 保留原字段 |
| AI 做了多少？ | Codex 辅助主要编码与测试编写；不把生成代码数量当作个人独立编码能力，说明你实际理解和验证的部分 |
| 还有什么没做？ | 多人服务、公式计算、大规模压测和真实客户试用等；这是有明确边界的本地演示项目 |

## 9. 三分钟现场演示

1. 30 秒：打开页面，说明多门店表格对齐和错误复核问题。
2. 30 秒：展开 north.csv 与 south.csv，展示“金额”和“Total”都映射到 amount。
3. 30 秒：点击校验，指出 12→5+7，不能只看合并行数。
4. 40 秒：在异常表找到订单 002，两条金额不同，因此都没有进入合格数据；再展示无效日期和“待确认”金额。
5. 30 秒：查看来源和修改记录，下载 Excel，打开 accepted / rejected。
6. 20 秒：说明基于哪个上游、具体新增什么，以及这些数字来自合成测试。

## 10. 你真正需要完成的理解练习

自己运行一次，回答：为什么 002 整组隔离？为什么 001 保留前导零？为什么金额合计不是所有输入相加？把金额上限改成 50 后，能预测通过记录从 5 变成 4 吗？

能脱离文档解释这四个问题，再用自己的话讲数据如何从文件走到报告，才算达到本项目的讲解目标。此项尚待你本人演练，不能由 Codex 替你宣布已经掌握。
