# 测试与交付审查

日期：2026-09-08。环境：Windows、Python 3.12.14，项目专用 .venv；完整版本见 requirements-lock.txt。

## 改造前 baseline

固定上游提交：7ca63acf2d2719fc3ffa36a386ff7172e67a5251。
修改 src 之前运行 tests/test_upstream.py：4 passed in 6.61s；git diff -- src 无修改。
验证原页面启动与带数据页面渲染、缺失值识别/中位数填补、原数据保留、变更记录、Excel 导出回读和空文件检查。不是对上游所有功能的全面验证。

## 分阶段结果

| 阶段 | 实际结果 |
|---|---|
| 原版对照 | 4 项通过 |
| 订单规则与上游回归 | 18 项通过 |
| 批量 I/O、映射、报告 | 32 项通过 |
| 中文页面、CLI、配置变更 | 36 项通过 |
| 边界情况补充 | 51 项通过，14.00 秒（一次本机测试耗时，非性能指标） |
| 依赖检查 | pip check：No broken requirements found |
| 真实 Edge 浏览器 | 合成演示、异常标签、ZIP 下载、两个 CSV 上传通过；未出现页面运行异常 |

浏览器第一次断言受 DOM 数字前后空格影响而失败，修正测试读取为去首尾空格后通过；业务数字始终一致。测试没有改成跳过错误。

## 已核验的演示结果

3 份合成输入（2 CSV + 1 XLSX），12 条；5 条 accepted，7 条 rejected，分为 1 条重复、2 条冲突、4 条无效；9 次字段标准化；accepted 金额合计 140.30。
命令行输出与浏览器下载包的 summary.json 一致。Excel 报告回读验证订单号为文本并保留前导零；公式样式文本不作为公式写入。报告包含 7 个 ZIP 文件、7 个 Excel 工作表。

第二个浏览器场景：上传 north.csv 与 south.csv，两份 CSV 共 8 条，2 条通过、6 条待复核，金额 10.30。不是通过内置演示绕过上传逻辑。

## 主要覆盖

- 正确数据、前导零、金额精确合计、严格日期、日月歧义。
- 空必填、坏金额、负数、超范围、精度超限。
- 完全重复、内容冲突、无效版本与有效版本同键冲突、不同门店同号。
- CSV/XLSX 混合、多工作表、Excel 原生日期、UTF-8 BOM 和分号分隔符、带换行 CSV 字段的记录号。
- 空文件、只有表头、重复表头、列数不一致、损坏 Excel、公式、控制字符、非 UTF-8。
- 映射缺失、别名歧义、手动映射、同一列重复映射、未知映射对象。
- 额外字段保留、来源及文件指纹、配置验证、规则修改后的结果失效、批次行数限制。
- Excel 回读、ZIP 内容、CSV 公式前缀防护、同输入规则重复业务结果、CLI 不覆盖输出、退出码。

## 严格审查

| 问题 | 结论与证据 |
|---|---|
| 能独立运行吗？ | 能；项目环境、启动脚本、CLI 和浏览器流程已运行，无 AI/API 账户要求 |
| 有明显 Bug 吗？ | 本次覆盖范围内未发现未解决的阻断缺陷；不承诺无任何 Bug |
| 新功能真的工作吗？ | 自动断言与真实上传/下载结果一致 |
| 有真实代码修改吗？ | 原 DataCleaner 增加 validate_order_batch；新增规则、批处理、页面、CLI、测试代码；Git diff 可核对 |
| 有工程价值吗？ | 新增业务规则、跨文件冲突处理、来源审计、配置重放和自动验证 |
| 只是换皮吗？ | 否；名称和中文界面之外，有上述可独立验证的处理功能 |
| README 完整吗？ | 包含运行、功能、截图、安装、结构、测试、来源与限制 |
| 用户能理解吗？ | 已交付通俗解释与四项理解练习；本人是否掌握仍待亲自演练，不能代替用户认证 |
| 能 Demo 吗？ | 能；内置已知答案、真实截图、浏览器端到端检查 |
| 能写简历吗？ | 可如实列为开源二次开发的个人项目；使用提供的真实素材，不声称商业成果或从零独立编码；面试前需本人演练 |

## 已知边界

本次完成范围为本地订单模式。未做真实客户数据试用、20,000 行极限压测、跨平台测试、在线多人部署或所有上游功能回归。限制值是资源保护配置，不是性能测量。
旧 .xls、GBK、宏和公式计算不支持；业务币种必须一致；文本 ID 的前导零可保留，原文件已丢失的零无法恢复。数据错误需要人工确认，不自动回写源文件。
