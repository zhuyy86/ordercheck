# Project 001：OrderCheck 订单批处理工作台

## 来源与选择

上游：Dendi Rivaldi (Louce) 的 Dataset Cleaner。
地址：https://github.com/Louce/dataset-cleaner-and-analyser-tools
许可证：MIT，保留原版权和完整许可文本。筛选依据保存于 Digital Nomad OS 的 research/github_project_shortlist.md。

## 原项目与架构

原项目上传单个 CSV/Excel，分析缺失值和异常值，提供统计填补、清洗、可视化与导出。
`src/app.py` 管操作界面；`src/data_cleaning.py` 的 DataCleaner 管清洗与质量信息；`src/data_visualization.py` 管图；`src/utils.py` 管类型识别与文件辅助操作。核心 Python 约数千行，单机运行。

主要不足：它围绕通用单表交互；缺少订单业务的多文件字段对齐、严格规则、逐条异常隔离和可重放的配置。统计填补适合某些分析，但不能据此猜订单号、日期或金额。

## 改造方案

保留原单表工作台、清洗算法和版权声明，并记录固定上游提交。在其 DataCleaner 上增加可调用的订单校验入口和独立批处理模块，并增加简洁中文工作台。

新增：
1. CSV/.xlsx 批量读入，按列名和可编辑映射对齐；字符串订单号保留前导零。
2. JSON 业务配置：别名、必填项、重复键、允许日期格式、金额范围；运行结果绑定实际配置。
3. 日期和金额严格校验；有问题的行保留在异常表，不默默删除或填零。
4. 跨文件重复键：完全相同记录可去重，内容冲突则整组隔离待复核。
5. 每行保留文件名、工作表/CSV 记录号、原始内容；输出清洁数据、异常、变更记录与统计。
6. CSV/Excel 报告下载、命令行批处理；合成案例和自动测试。

改后成为一个本地订单数据验收工具，用户能回答“哪些记录可以用，哪些要检查，来自哪里”。不做账户、云数据库、收费服务、AI 猜值、OCR 或超大数据工程。

## 技术栈

Python 3.12、pandas、openpyxl、Streamlit、pytest；保留上游所需 scikit-learn、SciPy 和 Plotly。依赖在项目专用环境安装，锁定实测版本。无需付费接口。

## 完成标准与测试

先验证未修改上游的启动和典型清洗函数，记录固定提交；再逐项实现、逐项测试。
必须能处理三份不同列名的合成订单表；正确识别错误日期/金额、缺列、重复与冲突；来源和统计可核对；空文件、坏文件明确报错；配置可重复使用；导出能重新打开；界面和命令行均可演示。
测试包括：核心规则单元测试、混合 CSV/XLSX 集成测试、上游回归测试、Streamlit 页面流程测试、实际本地启动与截图。所有量化结果只记录实测。

## 展示与交付

三分钟演示：加载合成案例→查看列名映射→执行→检查正确数据/异常/来源→下载报告。
完成 README、UPSTREAM.md、PROJECT_EXPLAINED_FOR_ME.md、测试报告和真实截图；根目录 portfolio/ 与 resume/ 各保存一份案例和中英文简历素材。项目标为基于开源的独立二次开发，由 Codex 辅助编码；不声称用户已经掌握尚未演练的能力。GitHub 首先达到可发布状态。
